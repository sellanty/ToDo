Зачем нужны: Маршруты определяют, на какой URL (эндпоинт) и какой HTTP-метод (GET, POST, PUT, DELETE) должен реагировать ваш сервер.

Пример для ToDo: Файл todoRoutes.js, который связывает маршруты (например, GET /api/todos) 
с соответствующими контроллерами (например, todoController.getAllTodos).