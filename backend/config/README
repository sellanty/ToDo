Зачем нужна: Здесь хранятся файлы конфигурации для всего приложения.

Что может быть внутри: Настройки подключения к базе данных, параметры JWT (секретные ключи, время жизни токена), 
настройки для разных сред (development, production) и т.д. Например, файл database.config.js или jwt.config.js.