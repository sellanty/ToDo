Зачем нужны: Содержат схемы и правила для валидации 
входящих данных (например, с помощью библиотеки Joi или Yup). Эти схемы затем используются в middleware для проверки.

Пример для ToDo: todoValidator.js с правилами, что title обязателен и должен быть строкой.